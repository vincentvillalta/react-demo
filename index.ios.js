/**
 * Sample React Native App
 * https://github.com/facebook/react-native
 * @flow
 */

import { AppRegistry } from 'react-native'
import AppContainer from './source/AppContainer'
AppRegistry.registerComponent('homeSearch', () => AppContainer)
